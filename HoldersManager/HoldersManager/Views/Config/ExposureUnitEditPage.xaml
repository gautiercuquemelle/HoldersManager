<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:cmp="clr-namespace:HoldersManager.Components"
             x:Class="HoldersManager.Views.ExposureUnitEditPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Icon="search.png" Text="Save" Command="{Binding SaveCommand}"/>
        <ToolbarItem Icon="search.png" Text="Cancel" Command="{Binding CancelCommand}"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <cmp:EditConfig/>
    </ContentPage.Content>
</ContentPage>